#+TITLE: Historic Data ADR-020
#+STARTUP: overview num
#+TODO: TODO | DONE

Here are the principals I want to keep that are important now.

* DONE Dukascopy will not be used
It has proved to be a bit problematic and not as complete as OandA

* DONE OandA Forex Pairs
** DONE Existing OandA Forex Pairs will be used
We have downloaded a lots of OandA raw Forex price data in CSV format
that will continue to be used.
** DONE XAUUSD is out of scope
XAUUSD is present in the existing corpus but we have no way to trade
it through OandA, so the data is not needed.  It is not ingested.  The
25th instrument in the corpus is therefore intentionally skipped, and
every instrument M2 handles is an FX pair.

** TODO Derived / Conanical Data needs to be defined
If the existing derived .CSV format differs it will be re-derived
** DONE Conanical format will be immutable and versioned
If any formatting needs to be changed or gaps need to be filled the
new data will be rebuilt if needed and reversioned.

The version identifier lives in the manifest, or in a header of the
derived CSV file.  It does not appear in the path.

Old versions are not retained.  A reversion presumably means the prior
derivation was wrong, so the old data is invalid, and so is any
backtest or analysis that used it.  The old version is wiped.
Retaining prior versions may be added later as a configuration item;
for now the assumption is that old versions are thrown away.

** DONE We may keep a manifest
Settled by issue #73 (branch =feature/73-manifest=): =marketdata.Manifest=
is the collection-level identity, provenance, and revision record for a
canonical =BarSet=.  =Bar=/=BarSet= (#72) hold observation-local facts;
=Manifest= holds why the dataset is trustworthy and what produced it,
independently of any filesystem path.

Fields: =Provider= (opaque logical provider name, e.g. ="oanda"=),
=Instrument=, =Interval=, =Span= (half-open partition range), =Basis=;
=SchemaVersion=, =RawFingerprint= (hex-encoded content fingerprint of the
raw source artifact(s)), =BuilderVersion=, =ValidatorVersion=,
=ResamplerVersion= (="none"= when not derived — never left empty, so
empty always means "not yet set"), =CalendarVersion=; =BuiltAt= (from an
injected clock, never =time.Now=); a lightweight coverage summary
(=BarCount=, =FirstBar=, =LastBar=) — not the full Coverage/Gap model,
which stays with #79; and =Parent *ParentRef= for a derived/resampled
dataset, =nil= for a dataset built directly from raw.

=Manifest= has no stored "Revision" or "ID" field a caller could set
inconsistently with the rest of the record.  =Manifest.Revision()
string= computes a deterministic hex-encoded sha256 fingerprint from
every other field on demand: two independently built manifests with
identical inputs always produce the same revision, and changing any one
field except =BuiltAt= — including =Parent= — changes it.  =BuiltAt= is
excluded on purpose: it is Manager's own build-time provenance, not part
of dataset identity, so two datasets built from identical inputs at
different times must still agree.  The hash input is an unambiguous
JSON-encoded struct (fixed field order, no map, every string properly
quoted/escaped) tagged with an explicit encoding version, not a
hand-joined delimited string — a delimited encoding let one field's
content be crafted to collide with an adjacent field's key, which a
design review caught before merge.  A canonical file header or store
records this computed string as the dataset's version identifier,
matching the principle above that the version identifier lives in the
manifest or a file header, never in the path.

=Parent= carries the parent dataset's instrument, interval, and its own
=Revision()= string at build time — not an embedded parent =Manifest= —
so lineage does not force retention of a superseded revision; if the
parent is later rebuilt, the recorded revision simply stops matching the
parent's current one, which is the intended staleness signal.  A
Manifest's =Parent= and =ResamplerVersion= must agree with each other:
=Parent= is nil if and only if =ResamplerVersion= is the literal
=="none"==.  When =Parent= is set, it must name the same instrument as
the child and an interval strictly finer (shorter nominal duration) than
the child's own =Interval= — a resampled dataset only ever derives a
coarser interval from a finer one, so any other relationship is rejected
as impossible lineage rather than merely unverified.

=Manifest.Matches(bs BarSet) error= is the manifest/data pairing check:
instrument, interval, and basis must agree; =Span= must agree;
=BarCount= must equal =len(bs.Bars)=; and — closing a design-review gap
in the first draft — =FirstBar=/=LastBar= must agree with the actual
first and last bar's =Time= in =bs.Bars= (both zero when =bs.Bars= is
empty).  =Manifest.Validate()= checks the manifest's own structural
invariants independently of any =BarSet=: non-zero/non-empty required
fields, a positive =SchemaVersion=, a =RawFingerprint= matching the
required =<algorithm>:<hex digest>= form, a coverage summary consistent
with =BarCount= (empty means a zero range, exactly one bar means
=FirstBar= equals =LastBar=, more than one requires a strictly
increasing range), and the =Parent=/=ResamplerVersion= lineage rules
above.

This issue is the model only.  =marketdata.Manager= has no build/publish
operation to attach a =Manifest= to yet; that is later M2 work.

** TODO Day and week alignment needs to be resolved.
We may download any missing interval data from OandA and derive it
from that source vs. from earler sources.  
** DONE OandA Volume represents ticks at OandA
It is not volume by any means but may serve as a proxy for how active
a pair was at the exchange OandA gets it data from.  It can be ignored
if it is not desired, but it will be included in the derived data
** TODO Avg and Max Spread will also be kept in derived format
OandA provides a bid and an ask for each of O, H, L and C.  Max and
avg spread will be calculated from those values.

#72 settled the *representation* on =Bar=: =AvgSpread= and =MaxSpread=
are both =num.Price=.  It also settled the build-from-raw *formula*,
which is the only one reconstructible from raw's bid/ask OHLC (raw
preserves no per-tick spread stream, so the legacy tick-weighted
variant in =data_manager.go= is off the table): the per-corner spread
is =(ask - bid)= at each of O, H, L, C; =AvgSpread= is the mean of
those four and =MaxSpread= the max of those four.

Still open, and deferred to M5 not #72: what avg spread *means* for the
simulated-broker fill model, and the aggregation rule when a derived
interval is resampled from a finer one (#81).  Representation is fixed;
meaning is not.
* DONE Raw Provider data will be saved separately

All new data downloaded will be store separate in it's original
pristine format, in a completely different directory structure than
the derived data.

* DONE We will use the term Bar (not candle)
And Tick, Trade if/when the time comes

* DONE marketdata.Manager will be the exported access to data

marketdata.Manager will be referred to as datamanager or DM for short.

That is ALL data consumers MUST go through the marketdata.Manager.
marketdata.Manager is the only entity that may request downloads from
providers or initiate canonical historic data builds.

                 +-- Download Manager -- Data Provider
                 |                 
  Consumer -> DM +-- Data Store  -- Storage Mechanism (Memory, CSV, Parquet)
                 |
                 +-- Bar Maker

Everthing behind the data manager will live in a 'internal' directory.
No other package is allowed to retrieve, build or store data, all must
go through the DM.

** DONE The boundary is non-negotiable and closed to outside implementation

The rule above is stronger than "consumers should prefer the DM".  No
package outside the =marketdata= hierarchy may implement, supply, or
even name a provider or storage seam.  Stated normatively:

#+BEGIN_QUOTE
The =marketdata= package is the sole public entry point for historical
market data.  No package outside the =marketdata= package hierarchy may
directly access, construct, configure, inject, or depend upon a
market-data provider, storage implementation, data-normalization or
derivation mechanism, cache, or persistence format.  These are private
implementation details owned by =marketdata= and packages beneath its
=internal= boundary.

A consumer may request data through =marketdata.Manager=.  If provider
choice must be exposed, the consumer may select a provider only by an
opaque logical name or identifier -- for example, =oanda=, =alpaca/iex=,
or =alpaca/sip=.  That selection must not expose a provider interface,
concrete provider type, credentials model, transport, or construction
mechanism.

Storage is never selected, supplied, or accessed by a data consumer.
The manager owns all decisions concerning storage, persistence layout,
canonicalization, aggregation/derivation, caching, and retrieval.

This restriction is intentional.  Relaxing it later requires an explicit
architectural decision and refactor; it must not happen incrementally by
exporting implementation seams.
#+END_QUOTE

Consequences:

- =Config= supplied to =marketdata= carries only consumer-level policy
  and selections.  If provider choice must be exposed later, it is an
  opaque logical name -- never a provider or storage implementation, an
  interface, a concrete type, a credentials model, or a construction
  mechanism.
- There is no externally implementable provider or storage injection
  seam.  Any such seam an implementation adds is a violation of this
  boundary, not an extension point.
- The boundary is enforced mechanically where Go's package rules allow
  it (an architecture test forbidding =marketdata= from importing
  provider/storage/vendor packages, and the =internal= boundary keeping
  the implementations un-importable from outside).

** DONE DM will provide a memory cached version of data
DM will handle all data requests, caching data as it is programmed.
The cache cleaning protocol will be defined during implementation.

** DONE Store root will be a configuration item
* DONE Determine the marketdata.Bar structure
Settled by issue #72 (branch =feature/72-marketdata-bar=).

Two levels, as anticipated:

- =marketdata.Bar= (per observation): =Time= (authoritative observed
  open, stored verbatim, never index-reconstructed -- the #179 DST
  lesson), bid-basis =Open/High/Low/Close= (=num.Price=), =AvgSpread=,
  =MaxSpread= (=num.Price=), and =Ticks= (plain =int64=, not
  =num.Quantity= -- ADR-004 defines no count type).
- =marketdata.BarSet= (collection): the homogeneous metadata shared by
  every bar -- =Instrument=, =Interval=, =Span= (half-open =TimeRange=),
  and =Basis= (=PriceBasis=) -- so it is never repeated per Bar.

Both are plain records with exported fields, validated via =Validate()=
at the normalization/store boundary rather than through a constructor,
per the architecture document's "plain data transfer structs may remain
literals" convention. =Mid()= and =Range(interval, calendar)= are
derived getters; mid and the half-open range are computed, not stored.

Dataset revision/provenance stays with the manifest (#73) and
coverage/gap detail with coverage (#79); =BarSet= references those
levels rather than absorbing them.

** DONE How will gaps and closed market days be handled
Fixed index with dummy data as done in the trader-first-try will not
be supported.

Settled at the =Bar= level by #72: a Bar existing *is* an observation,
so a missing interval is an *absent* Bar -- there is no zero-filled
dummy row and no per-Bar status flag. Which intervals are expected,
closed, or missing is a coverage concern (#79), and the
provider-declared-incompleteness distinction below (OANDA's =complete=
flag versus a calendar-derived gap) is resolved at normalization (#76),
not by persisting incomplete bars.

* DONE Data at rest will be stored as it has been.
The current path will be something like it is now
	.../data/raw/provider/inst/yyyy/mm/inst-yyyy-mm-intvl.csv
	.../data/derived/provider/inst/yyyy/mm/inst-yyyy-mm-intvl.csv
Furture data format such as Parquet if we implement will be determined
when it is implemented

* DONE All derived data will be persisted
All data that is downloaded or derived will be persisted

* DONE Livefeed will not be included in M2
Title says it all
* TODO Where does FX holiday data come from?
Refer to trader-first-try for fx-holidays and closed datys.  Need to
retrieve it from source and decide how it is represented here.

Candidate source, located but not yet adopted:
=trader-first-try/market/forex_hours.go=, functions
=isForexMarketClosed= and =isMajorForexHolidayClosed=.  It works in
=America/New_York=, which lines up with ADR-012's 17:00 NY rollover.
The rules are:

- Jan 1, Dec 25, Dec 26 -- closed all day
- Dec 24, Dec 31 -- closed from 13:00 onward
- Sunday evening session stays closed when Sunday itself, or the
  following Monday, is one of the three full-day holidays

Note it is hardcoded rules, not a data file, and it covers only those
five dates -- no Good Friday, and no handling of holidays that shift
when they fall on a weekend.  Decide whether to carry that set forward
as-is or source a real calendar.

* TODO Review comments to work in

Parked here from review.  Work them into the sections above during the
next edit and delete this section.

** DONE Volume's Go type belongs to the Bar structure work
Settled by #72: =Bar.Ticks= is a plain =int64= (not =num.Quantity=),
and =Ticks= is the single name used going forward. The raw tree's
=volume= column is normalized to =Ticks= at read time; both trees'
values are the same OANDA tick count.

** TODO State that raw is authoritative
It is strongly implied by "raw saved separately, pristine" plus
"canonical is rebuilt and reversioned", but never actually written
down.  The entire rebuild story rests on it, so it deserves a
sentence of its own.

** TODO Do not delete the existing candle-v2 tree until it has been diffed
Once the new canonical build exists, diff it against the old
=candle-v2= tree before deleting anything.  Bid OHLC only, tolerance
about one unit at 1e5 scale -- legacy built values through
=types.PriceFromFloat=, so last-digit differences are expected and
byte-for-byte parity is neither achievable nor the goal.

The point is catching gross errors: off-by-one slot indices, DST
drift, wrong instrument mapping, missing months.  It is a 21-year
regression test and it is only available once.  This does not conflict
with the no-retention policy above -- that policy governs superseded
/new-format/ versions, not the one-time legacy comparison.

** TODO Provider-declared incompleteness is separate from gaps
Raw carries OANDA's own =complete= column, and legacy silently skipped
rows where it was false (=datamanager/data_validation.go=).  That is a
different thing from a calendar-derived gap: one is the provider
saying "this bar had not closed yet", the other is Trader inferring
"data should exist here and does not".  Fold the distinction into "How
will gaps and closed market days be handled" so it does not get lost.

** TODO Say that third-party data sources are deferred
"Everything behind DM lives in =internal/=" plus "no other package may
retrieve, build or store data" means an outside user cannot supply a
data source.  The architecture document promises exactly that seam via
=marketdata.HistoricalSource= and =LiveFeed=.  For M2, OandA-only,
deferring it is the right answer -- it just needs to be stated, since
the promise exists elsewhere.

** TODO Resolve W1 alignment before implementation starts
Still correctly open under "Day and week alignment", but it is a short
check against the OandA API rather than a design question: does
=weeklyAlignment= accept Sunday?  ADR-012 defines the FX week as
opening Sunday 17:00 New York, and =weeklyAlignment= is believed to
default to Friday.  The answer decides whether W1 is provider-native
like the other four intervals or the single derived one, which shapes
the interval materialization work.

Related, and already verified: the existing D1 corpus opens at 21:00Z
in July and 22:00Z in January -- both 17:00 America/New_York.  That
matches ADR-012.  But the legacy client sends no alignment parameters
at all, so that boundary came from OandA's request defaults
(=dailyAlignment=17=, =alignmentTimezone=America/New_York=).  The new
downloader should pin them explicitly rather than inherit them.

** TODO Typos, for whenever this folds into adr-decisions.org
"Conanical" (x3), "principals" -> "principles", "Furture", "datys",
"earler", "Everthing".  OANDA is usually all-caps.


