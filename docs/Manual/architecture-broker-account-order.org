#+TITLE: Broker / Account / Order Architecture

* Status

- Phase 1 (=account.Broker= → =account.Ledger= rename, new =account.Order=,
  new =brokers.Broker= interface + compile-time proof =*oanda.Client=
  satisfies it) — done, commit =8c4adaf=.
- Phase 2, originally scoped to =service/orders.go= only — done, commit
  =eaae1a6=. =Account.broker()= narrows =a.svc.OANDA= to =brokers.Broker=.
  The rest of =service/='s execution/account-state calls that still
  reached the concrete client directly were finished as part of Phase 4's
  chunk 5 (commit =d3e4bb2=) — every non-data-shaped call in =service/=
  now goes through =a.broker()=; only =GetPricing=/=StreamPricing= remain
  on the concrete client, correctly deferred to =DataProvider= (not yet
  defined).
- Phase 3 (below) — *done*. The =service.Account= merge (chunk A, commit
  =2f4dc1d= — per-account state/methods; chunk B, 2026-07-21 — the
  =Account/Accounts/FirstAccount/DefaultAccount= session registry, as
  =account.Registry=) is complete; =service.Account= no longer exists.
  The =account.Ledger= merge ("Ledger merges into Account too" below —
  =Ledger= does not survive as a separate wrapping type, and =Account=
  does *not* hold a =Broker= field, correcting this doc's earlier
  wording) landed earlier, as Phase 4's chunk 3 (commit =59e4869=) since
  exploring chunk 2 surfaced it as a prerequisite.
- Phase 4 (below) — reshaped 2026-07-22 mid-discussion: order fills
  (including stop/take triggers) are events the *Broker* emits
  asynchronously via =StreamTransactions=, not something a synchronous
  =Ledger.SubmitOpen= call decides on the spot. Chunks 1–5 done, commit
  =c1615d4= for chunk 4 — backtest now runs on real =Broker= fills and
  async stop/take events end-to-end, verified against a fresh baseline
  (trade-by-trade, not old committed baselines — see the baseline caveat
  under chunk 4 below). Chunk 5, commit =d3e4bb2= — narrowed after
  tracing the code: live trading has no local ledger by design (OANDA is
  authoritative), so it doesn't need chunk 4's fill-bridge; scoped
  instead to routing every remaining execution/account-state call in
  =service/= through =brokers.Broker= consistently. Chunk 6
  (=AccountSnapshot='s role) resolved as moot, no code change — its
  premise assumed live would adopt chunk 4's fill-bridge, which chunk 5
  ruled out; see Phase 4's chunk 6 entry below for the trace. Chunk 7
  (risk/sizing math onto =Account=) done, commit =ff37eb4= — live's
  =PlaceMarketOrder= now sizes via the same =account.SizePosition=
  math backtest already used, instead of a separate hand-rolled float
  implementation; see Phase 4's chunk 7 entry below for the trace and the
  two intentional behavior changes (live now gets a margin check, and
  under-minimum sizes are rejected instead of floored to 1 unit).

This doc describes the target shape so it survives the refactor and can
fold directly into the manual's Architecture section once implemented —
see =docs/Plans/service-refactor.org= for the related =service.Service=
god-object refactor this unblocks.

* Ownership hierarchy

A =Broker= is an execution venue. A =Broker= can have more than one
=Account=. An =Account= holds =Orders=, =Positions=, and =Trades=. This is
a strict three-level ownership chain:

#+begin_example
Broker
  └─ Account
       └─ Order / Position / Trade
#+end_example

Market data is explicitly *not* part of this chain. Historical candle data
and live price/tick streaming are independent of any account — some data
providers (Dukascopy) aren't brokers at all, and even for a vendor that is
both (OANDA), live streaming is conceptually =DataProvider='s
responsibility, not =Broker='s, even though the bytes happen to arrive over
the same connection. See =docs/Manual/DataManager.org= for the data side;
this doc only covers execution.

* Package layout

Flat at repo root — no =providers/= or =internal/= umbrella (considered and
rejected; see =docs/Plans/service-refactor.org= for why). =brokers/= and
=account/= already exist at root today and stay there.

#+begin_example
brokers/
  broker.go              -- Broker interface: PlaceOrder, CloseTrade,
                             GetAccountSummary, StreamFills, ...
  oanda/                  -- concrete Broker implementation (exists today)
  sim/                    -- concrete Broker implementation (exists today)
  alpaca/                 -- future
  interactivebrokers/     -- future

account/
  account.go               -- Account (domain type: an account at a broker)
  ledger.go                -- Ledger (renamed from account.Broker — see below)
  order.go                 -- Order (new: canonical, broker-agnostic)
  trade.go, trade_position.go, trade_lot.go, fill.go  -- existing, unchanged
#+end_example

** The =Broker= interface

Doesn't exist yet. Lives in =brokers/broker.go=, satisfied by =oanda.Client=
and =sim.Simulator= (and, eventually, =alpaca=/=interactivebrokers=
implementations). Only covers execution — placing/closing orders, reading
account state, streaming fills/transactions. Deliberately excludes
candle/price history: a broker that also happens to serve market data
(OANDA) satisfies =DataProvider= as a *separate* interface, not as part of
=Broker=. Resist adding data-fetching methods to =Broker= just because one
concrete implementation happens to support both.

** Renaming =account.Broker= → =account.Ledger=

=account/broker.go= currently defines a =Broker= struct — but it's the
in-memory bookkeeping ledger (open lots, balance tracking), used by both
backtest (=account.NewBroker("sim")=) and, implicitly, live position
tracking. That's a different concept from the =brokers.Broker= interface
above (an execution venue), and having both named "Broker" one package
apart (=account.Broker= vs. =brokers.Broker=) is a landmine for anyone
reading imports. Renaming the ledger to =account.Ledger= frees "Broker" to
mean only the execution-interface concept, consistently, everywhere.

** New: =account.Order=

No broker-agnostic =Order= type exists today. Live code
(=service/orders.go=) talks directly in OANDA's vocabulary —
=oanda.CloseTradeResult=, =oanda.OpenTrade= — which means every additional
broker would require its own request/result shapes threaded through every
service method that touches orders. =account.Order= becomes the canonical
shape; each =brokers/<name>= package is responsible for translating
=account.Order= to/from its own wire format on the way in, and translating
fills back into =account.Fill=/=account.Trade= on the way out.

=Order= lives in =account/=, not its own top-level package: an order
belongs to an account tightly enough that a fourth root package felt like
more ceremony than the size warrants.

* Why broker/account first, not the Service split

=service/account_*.go='s =Account= (a live per-session cache/wrapper —
distinct from =account.Account=, the domain type above; see the naming
note below) currently holds a raw =*oanda.Client= via a back-reference to
=*Service=. The Service/=Runnable= split (=docs/Plans/service-refactor.org=)
can't be designed properly without first knowing what a decoupled
=service.Account= should depend on instead — and that answer is
"=brokers.Broker=," which doesn't exist until this refactor lands.
Sequencing broker/account first means the Service split is informed by a
real interface instead of a guess, and de-risks the larger, messier
refactor by landing the smaller, self-contained one first.

** Naming note: three things currently called "Account"

Worth being explicit about, since it's a live source of confusion during
this refactor:

1. =account.Account= (root =account/= package) — the domain bookkeeping
   type: balance, open lots, PNL. Broker-agnostic.
2. =service.Account= (=service/account_session.go=) — a live per-request
   session cache/wrapper: holds an ID, a back-reference to =*Service=, and
   an =AccountSnapshot=. This is the thing the god-object refactor is
   about; unrelated to (1) except by name.
3. =oanda.AccountSummary= / =oanda.AccountDetails= (=brokers/oanda=) — wire
   DTOs from OANDA's API.

(3) stays a wire DTO, untouched. (1) and (2) are **merging** — see Phase 3
below — resolving the ambiguity by deciding there should only be one
=Account= type, not by permanently keeping two.

* Phase 3: merge =service.Account= into =account.Account=

Decided 2026-07-21. Split into two chunks (chunk A done 2026-07-21; chunk B
— relocating the registry — not yet started) after exploration showed the
full merge was larger than any prior chunk and found a real scope
question: two of =service.Account='s methods, =StartBot=/=ListBots=,
turned out to be Service-registry-scoped (=Service.bots=/=botsMu=,
=Service.BuildLiveStrategy=), not per-account state. Per the user: "the
bots will affect account state through the order mechanism but not
anything directly related to accounts" — confirmed they should stay on
=Service=, not move onto =Account=.

Rather than keep the domain ledger (=account.Account=) and the live
session wrapper (=service.Account=) as two separate types that happen to
share a name, they become one =account.Account=.

** Chunk A — done 2026-07-21: type + per-account methods merged

=account.Account= gained the live-session fields (=OANDA=, =Log=,
=snapMu=/=snapshot=) and a =NewSession(id, oanda, log)= constructor
alongside the existing =NewAccount(name, deposit)= ledger constructor —
the two populate disjoint field subsets (live sessions carry no ledger
state; backtest ledgers carry no OANDA/Log/snapshot state). =AccountSnapshot=
moved into =account/= unchanged (confirmed zero back-reference to
=*Service=). All genuinely per-account methods moved with it:
=EnsureSnapshot=/=getSnapshot=/=broker= (→ =account/session.go=),
=GetAccountSummary=/=GetTransactions=/=StreamTransactions= (→
=account/broker_calls.go=), =PlaceMarketOrder=/=CloseTrade=/
=UpdateTradeStop=/=ListOpenTrades= (→ =account/orders.go=, including
chunk 7's sizing types), =RunLiveJournal= (→ =account/journal.go=),
=RunLiveStrategy=/=runOneTick=/=runPricingStream=/=seedTickCounts= plus
=LiveRunConfig=/=LiveStrategy=/=LivePrice=/=LiveTrade=/=LivePlan=/
=LiveOpenRequest= (→ =account/live_runner.go= + =account/live_types.go=),
=GetPrices= (→ =account/prices.go=).

Two dependencies fell out of the same "not really per-account" principle
as =StartBot=/=ListBots= and got the same treatment — injected as
parameters instead of reached via a back-reference to =*Service=:
=RunLiveJournal= gained a =botIDLookup func(tradeID string) string=
parameter (=Service.RunLiveJournal= passes =s.LookupTradeBotID=);
=LiveRunConfig= gained a =RegisterTradeBotID func(tradeID, botID string)=
field (=Service.startBotOn= passes =s.RegisterTradeBotID=, the only
caller that sets =BotID=).

=service.Account= is gone; =Service.Account=/=Accounts=/=FirstAccount=/
=DefaultAccount= (unchanged in this chunk — chunk B moves them) now
construct/return =*account.Account= via =account.NewSession=.
=Service.StartBot= folds its old =(a *Account) StartBot= body into an
unexported =Service.startBotOn(ctx, acc, cfg)=, parameterized by
=*account.Account= instead of a receiver; a new
=Service.StartBotOnAccount= covers the account-scoped REST/MCP callers
that used to call =acc.StartBot= directly. =(a *Account) ListBots=
became =Service.ListBotsForAccount(accountID)=, same filter logic.

Unplanned but necessary: moving =Account.broker()= (needs =brokers.Broker=)
into =account/= surfaced a latent import cycle — package =brokers=
imported =brokers/sim= only for a compile-time =var _ Broker =
(*sim.Sim)(nil)= assertion, and =brokers/sim= imports =account=, so
=account= → =brokers= → =brokers/sim= → =account= cycled. Fixed by moving
that assertion (and =PriceUpdater='s) into =brokers/sim= itself, asserted
against the interface from the implementing package — the standard
direction, and =brokers/sim= already depended on =account= regardless.

Verified: =go build ./...=, =go vet ./...=, =go test ./...= all clean
across the whole repo (this chunk's blast radius reached =cmd/order=,
=cmd/serve=, =cmd/live=, =api/rest=, =api/mcp= via the moved
=PlaceMarketOrderRequest=/=OrderProposal=/=GetPricesRequest=/=PriceInfo=
types). Test files moved alongside their production code
(=account/orders_test.go=, =account/live_runner_test.go=,
=account/account_snapshot_test.go=); =service/account_session_test.go=
stayed in =service/= since it tests the registry, which didn't move this
chunk.

** Known collision to resolve: =Account.ID= — resolved in chunk A

=account.Account= already had an =ID= field
(=account/account.go=, set via =idgen.NewULID()= in =NewAccount=) — but
it's an internally-generated ULID, purely a ledger-instance identifier.
Grepped: it is never read outside =account/= itself, and every non-test
reference is a hardcoded literal (=ID: "test"=) — it is not load-bearing in
backtest or engine today.

=service.Account.ID=, meanwhile, *is* load-bearing — it's the real OANDA
account identifier string (e.g. =101-004-12345-001=) threaded through
every live broker call (=a.broker().GetOpenTrades(ctx, a.ID)= and
similar).

These are different concepts sharing a field name. Resolution (implemented
in chunk A): =NewSession(id, oanda, log)= sets =ID= to the live/broker
account identifier directly; =NewAccount(name, deposit)= keeps assigning
its ULID unchanged — confirmed harmless since nothing reads it in the
backtest path. No actual collision at runtime since the two constructors
are never both used for the same instance.

** What the merged =account.Account= needs — done in chunk A

Beyond the existing ledger fields (=Balance=, =Equity=, =Lots=, =Trades=,
=RiskFraction=, ...), the live-session half needed:

- *No =Broker= field.* Corrected 2026-07-22: ownership points the other
  way — "a Broker can have more than one Account" (this doc's own opening
  hierarchy), and every =brokers.Broker= method already takes =accountID=
  as a parameter, confirming the interface itself is shaped as "one broker
  addressing many accounts," not "one account holding one broker." What
  submits an order needs a =Broker= handle to call, but that handle isn't
  something =Account= owns — see Phase 4 for where it actually lives.
- =Log *slog.Logger= — replaces =a.svc.Log=. Added.
- The =AccountSnapshot= cache (=snapMu=/=snapshot=) — moved over unchanged;
  it already had zero back-reference to =*Service= (see
  =docs/Plans/service-refactor.org='s Phase 1 note — this was already
  identified as the one piece with no cycle risk). Its role narrows once
  Phase 4's event-driven fills land — see Phase 4's source-of-truth note.
- Pricing/streaming (=GetPricing=, =StreamPricing=, used by
  =prices.go=/=live_runner.go=/=pip_values.go=) stays on the concrete
  =*oanda.Client= — added as an =OANDA *oanda.Client= field directly
  (not yet a =DataProvider= field, that interface still doesn't exist) —
  same deferral as Phase 1/2.

** Ledger merges into Account too — done, Phase 4 chunk 3 (commit =59e4869=)

Decided 2026-07-22, prompted by a real-world sanity check: in standard
bookkeeping, a *ledger is something an account has* — the record of its
transactions — not a container an account lives inside. =account.Account=
already kept most of its own ledger directly: =Trades []*Trade= (closed
transaction history), =Lots=/=AddLot=/=CloseLot= (position bookkeeping).
What the separate =account.Ledger= wrapper (=account/ledger.go=, the
struct Phase 1 renamed from =account.Broker=) added on top was really just
the event queue (=evtQ=, =Events()=, =publishEvent=) and =SubmitOpen=/
=SubmitClose= as entry points wrapping =AddLot=/=CloseLot= with a
synchronous event fire — implementation plumbing, not a distinct
real-world concept.

=account.Ledger= as a separate type is gone — confirmed during chunk A's
exploration: no =account/ledger.go= file exists; its method set
(=SubmitOpen=/=SubmitClose=/=Events=/=publishEvent=/etc.) now lives
directly on =account.Account= via =account/events.go=. =engine.Trader=
(=engine/trader.go=) embeds =*account.Account= directly, not
=*account.Ledger=.

** Where the registry goes — done, chunk B, 2026-07-21

=Service.accounts map[string]*Account=/=accountsMu=/=firstID= and the
=Account()=/=Accounts()=/=FirstAccount()=/=DefaultAccount()= logic moved
into =account/registry.go= as =account.Registry= — =service.Service= keeps
only a thin facade (=service/account_session.go=, now four one-line
delegations) delegating to it, the same shape as =service/review.go=
delegating to =reviewsvc=.

One deviation from the plan as first written: =Registry= does *not* hold
an =OANDA=/=Log= field of its own. Its methods (=Account=/=Accounts=/
=FirstAccount=) take the OANDA client and logger as parameters instead,
supplied by =Service= on every call. Reasons, found while designing this
chunk:

- Several existing tests construct =Service= via a bare
  =&Service{OANDA: ..., AccountID: ...}= struct literal, bypassing
  =New()=. The old =accounts map= worked in that case because it
  lazily self-initialized on first use. A =Registry= built via a
  =NewRegistry(oanda, log)= constructor, stored as a field =Service= must
  remember to populate, would nil-pointer-panic in every one of those
  tests. Taking the client/log as parameters keeps =Registry='s zero
  value fully usable — =service.go='s =registry account.Registry= field
  needs no constructor call in =New()= at all, exactly like the map it
  replaced.
- It's also more faithful to the pre-move behavior than caching
  client/log once would be: the old =&Account{ID: id, svc: s}= read
  =a.svc.OANDA=/=a.svc.Log= live through the =svc= pointer on every call,
  not a value frozen at cache-insert time.

=ResolveAccount= (resolves/validates the *configured default*,
=Service.AccountID=) stays on =Service=, distinct from the registry (a
*cache of already-resolved sessions*) — =DefaultAccount= still composes
the two, unchanged. No external caller (=cmd/order=, =cmd/serve=,
=api/rest=, =api/mcp=) needed any changes — =Service='s method signatures
are unchanged, only their implementation moved. Verified:
=go build ./...=, =go vet ./...=, =go test ./...= (full repo, plus
=-race= on the concurrency-safety test) all clean.

Phase 3 is now complete — =service.Account= is gone, =account.Account= is
the one merged type, and =service.Service= holds no account-session state
of its own.

** Naming: =service/account= vs. =./account=

This phase does *not* introduce a new =service/account= package — the
session registry and merged =Account= type live directly in the existing
top-level =account/= package alongside =Ledger=/=Order=/=Trade=. No
same-package-name-different-import-path situation to manage (unlike
=review= vs. =service/review='s =reviewsvc= alias) — there is only one
=account= package once this lands.

* Phase 4: converge backtest and live onto one execution flow

Decided 2026-07-22, not yet implemented. Broader than Phase 3's type merge:
the goal is that backtest and live trading are *the same flow*, differing
only in which =Broker= is plugged in and where source-of-truth authority
sits — not two systems that happen to share a struct name. Explicit
principle from this discussion: *correctness over convenience* — this is a
real, larger refactor, accepted as such, to be taken one chunk at a time
rather than deferred for being big.

** The problem: backtest and live don't share a path today

- *Backtest*: =backtest/execute.go= computes the fill price itself (candle
  close, adjusted for spread/slippage via =account.FillAdjust(isBuy,
  candle.AvgSpread, slippage)=) and calls =Ledger.SubmitOpen=/=SubmitClose=
  directly. =SubmitOpen= trusts the caller's price outright and writes a
  =Lot= straight into =Account.Lots=. No =Broker= is ever consulted — the
  ledger write *is* the execution.
- *Live*: =service/orders.go= calls =a.broker().SubmitMarketOrder(...)=
  directly. OANDA executes and returns a real fill (which can differ from
  what was quoted). =account.Ledger=/=Account.Lots= is never touched — live
  has no local ledger today, relying entirely on querying OANDA
  (=AccountSnapshot=, =GetOpenTrades=) for state.

These are two independent code paths that both happen to end up at
something named =Account=. Merging the type (Phase 3) doesn't converge the
*flow* — =orders.go= would still call =broker()= directly on one side while
=execute.go= calls =Ledger.SubmitOpen= directly on the other.

** Target flow: orders are requests, fills are events

Reshaped 2026-07-22 from the original "=Ledger.SubmitOpen= calls =Broker=
synchronously" sketch. The trigger: a stop-loss is a *resting* order
sitting on the broker's book — it doesn't fire when you submit it, it
fires later, whenever price actually reaches it, and only the =Broker=
knows when that happens. A market order is the degenerate case of the same
idea (the "delay" is ~0), not a fundamentally different mechanism. So
submitting an order and applying its fill to an account's ledger are two
separate steps, connected asynchronously — not one synchronous call.

- *Submission*: something (chunk 3's live-trading migration, or backtest's
  per-bar loop) calls =Broker.SubmitMarketOrder(ctx, accountID, ...)= to
  open, or sets a resting stop/take via =Broker.UpdateTradeStop=. =Broker=
  addresses accounts by ID (already true of every method in the phase-1
  interface) — it does not need an =Account= handle to do this, and
  =Account= does not hold a =Broker= field (see Phase 3's correction).
- *Fill application*: =Broker.StreamTransactions= (currently one of chunk
  1's deliberately-minimal =Sim= methods — see chunk 1 below) becomes the
  *real*, unified fill feed for both implementations: =oanda.Client=
  already streams real fills; =Sim= needs to actually implement it,
  emitting when =SubmitMarketOrder= fills immediately *or* when a resting
  stop/take it's tracking gets hit on a later =UpdatePrice= call. Something
  subscribes to this stream, translates =oanda.TxEvent= → =account.Event=,
  and applies it to the account's ledger (=AddLot=/=CloseLot=, now
  triggered by the event rather than called directly by =SubmitOpen=).
- *Stop/take triggering moves into =Sim=.* =backtest/exits.go='s
  =autoCloseExits= currently checks candle OHLC against
  =lot.Stop=/=lot.Take= and calls =SubmitClose= directly, outside any
  broker — doing the broker's job in the wrong place. That check belongs
  inside =Sim=, run against each =UpdatePrice=, mirroring how a real
  resting stop sits on OANDA's book and triggers on its own.

Backtest and live differ only in which =Broker= is plugged in and how it's
fed prices (candles via =TickFromCandle=, vs. real ticks) — not in how
fills get applied to an account, which is now the same event-consumption
step either way.

Known, explicitly parked difference: backtest replays candles, live
trading (real or paper) reacts to ticks. Not solved here — acknowledged as
a separate problem =brokers/sim= will need to handle, not a blocker to
this shape.

** Source-of-truth asymmetry — narrower than first thought

The flow converges, and this turns out to converge *authority* further
than the original sketch assumed too: if both =Sim= and real =oanda.Client=
apply fills to an account's ledger through the same
=StreamTransactions=-consuming step, there's no separate "write locally,
then reconcile" phase for trade fills at all — the event stream *is* the
single write path for both. What's left asymmetric is narrower:

- *Sim*: its own =StreamTransactions= emissions are authoritative by
  construction — nothing external to disagree with.
- *OANDA*: =StreamTransactions= (or an equivalent poll,
  =AccountSnapshot='s existing =GetAccountChanges= mechanism) is OANDA's
  own authoritative feed already — consuming it *is* the reconciliation,
  not a step bolted on after an eager local write. =AccountSnapshot='s
  remaining distinct job likely shrinks to continuously-updating figures
  that aren't tied to a specific fill event (unrealized PnL from price
  moves, margin) rather than reconciling the trade ledger itself — open
  detail for chunk 4, not resolved here.

** Risk management moves onto Account

=account.Account= already has an =RiskFraction= field, but only backtest
uses it (set once at construction, e.g.
=acct.RiskFraction = run.Request.RiskPct= in =backtest/executor.go=, never
touched again). Live's sizing math — stop distance, =quoteToUSDRate=, unit
sizing, =MaxUnits=/=MaxPositionUSD= caps — lives entirely inside
=service/orders.go='s =PlaceMarketOrder=, blind to the account beyond a
fetched equity number passed in per-call.

Target: this sizing logic becomes a method *on* =Account=, reading its own
=Equity=/=RiskFraction=/open =Lots= directly. Backtest and live then size
positions through the identical function given an identically-shaped
account, rather than two independent implementations that happen to agree
by coincidence. This is also the concrete reason =Account= needs to *own or
reference* risk management, not have it float in =service/=: risk
calculations are inherently dependent on account size and open positions,
which only =Account= actually has.

** Broker events converge too — now via =StreamTransactions=, not =Ledger.evtQ=

Original framing: "=Ledger.Events()= already exists, =live_runner.go=
should consume it too." Revised: the event queue itself moves — it's
=Broker='s job to know when a resting order triggers (see "Target flow"
above), so the queue belongs on =Broker= (via =StreamTransactions=), not
on =Ledger=/=Account=. =engine.Trader.StartBrokerEventHandler=
(=engine/trader.go=) and =service/live_runner.go= both end up consuming
the same kind of feed — a =Broker='s =StreamTransactions=, translated to
=account.Event= — rather than =live_runner.go= gaining a consumer for
=Ledger.Events()= as originally sketched. Same convergence goal, different
(now more accurate) mechanism.

** Sequencing (proposed, not yet agreed chunk-by-chunk)

Roughly dependency-ordered — later chunks build on earlier ones being
correct:

1. Make =brokers/sim= a real =Broker= (spread/slippage-aware fills from
   candle data). Foundational — nothing else can converge onto it until it
   exists. *Done, commit =472b398=* — six real methods
   (=SubmitMarketOrder=, =CloseTrade=, =UpdateTradeStop=, =GetOpenTrades=,
   =GetAccountSummary=, =GetAccountDetails=), four deliberately minimal
   ones split into =broker_stub.go=, plus =TickFromCandle= as the
   not-yet-wired foundation chunk 2 needs. Confirmed via
   =var _ Broker = (*sim.Sim)(nil)= in =brokers/broker.go=.
2. *Split into two, first half done.* =Sim.StreamTransactions= is real —
   emitting on immediate market-order fills *and* on resting stop/take
   triggers it now monitors internally via =UpdatePrice= (moved out of
   =backtest/exits.go='s =autoCloseExits=, which still runs unchanged for
   now — this half was scoped self-contained to =brokers/sim=, touching
   nothing else). *Done, commit =ebc5625=* — six methods gained event
   emission, =checkStopsAndTakes= added, full long/short × stop/take test
   coverage.
3. *Done, commit =59e4869=.* Merge =account.Ledger= into =account.Account=
   (see Phase 3's "Ledger merges into Account too") — purely structural,
   zero behavior change. =SubmitOpen=/=SubmitClose= and the event-queue
   machinery moved to =account/events.go= as =*Account= methods;
   =account.Ledger=/=NewLedger= deleted; =engine.Trader= embeds
   =*account.Account= directly and gained a nil-safe =Broker
   brokers.Broker= field for chunk 4 to populate;
   =backtest/executor.go='s =BrokerFactory= removed entirely (no separate
   =Ledger= left to construct). Full test suite green, confirming nothing
   about *what happens* on open/close changed — only which type the
   methods live on.
4. *Done, commit =c1615d4=.* Built the =StreamTransactions=-consuming
   step (=backtest/execute.go='s =drainBrokerFills=/=txEventToAccountEvent=)
   that applies fills to =Account='s ledger, replacing
   =SubmitOpen=/=SubmitClose='s synchronous =AddLot=/=CloseLot= calls, and
   wired =engine.Trader=/backtest onto real =Broker= calls via the
   =Trader.Broker= field (=Trader.Broker = sim.NewSimBroker(t.Account, nil)=
   — Sim shares Trader's own =Account=, no reconciliation layer). New
   =brokers.PriceUpdater= optional interface feeds =Sim= the bar's price
   (fused with its stop/take check) without adding data methods to
   =Broker= itself. =backtest/exits.go= deleted — stop/take detection is
   =Sim='s job now. This absorbed what was chunk 2's second half *and*
   most of what was chunk 6 (event convergence) — they turned out to be
   the same mechanism, not sequential steps.

   *Baseline caveat, applied*: compared against a *fresh* baseline
   captured on =main= immediately before this chunk (not old committed
   results) — confirmed necessary, since =backtest regress= against the
   committed =testdata/backtests/reports= baselines showed large
   pre-existing divergence even on unmodified =main=, exactly as the
   caveat predicted. Trade-by-trade diff post-change: trade counts match
   exactly for 3 of 4 sampled configs (the 4th, a tick-sensitive M1
   scalper, diverged more — plausible given the strategy's sensitivity to
   exact fill timing, not a red flag on its own); every price difference
   traces to a real fill replacing an estimate or exact stop-price
   triggering replacing end-of-bar OHLC approximation — not corruption.
   One real bug caught by this verification, not written correctly on
   faith: the fill-bridge initially looked up each open's =Lot= via
   =Account.Lots.Get()=, which raced against =Sim='s own stop/take-triggered
   closes (a lot could close before its *open* event was ever drained) —
   fixed by never depending on live =Lots= state in the bridge.
5. *Done, commit =d3e4bb2=.* Narrowed after tracing the actual code, with
   sign-off before proceeding: live trading has no local ledger by
   design — =live_runner.go='s strategy input is built fresh every tick
   straight from OANDA's own open-trades response, never touches
   =Account.Lots= — matching the source-of-truth principle below (OANDA
   is authoritative for live), not a gap to fix. Forcing live onto chunk
   4's =drainBrokerFills=/=txEventToAccountEvent= bridge would mean
   inventing chunk 6's reconciliation design for a path touching real
   capital, before that design exists. Scoped instead to consistency:
   =journal/live.go= gained a local =liveJournalClient= interface (same
   pattern =account_snapshot.go= already used, avoiding a
   =journal=↔=brokers= import cycle since =brokers/sim= already imports
   =journal=) so =NewLiveJournal= takes an interface instead of
   =*oanda.Client=; every remaining execution/account-state call in
   =service/= (=account_session.go=, =journal.go=, =live_runner.go=,
   =account.go=, =portfolio_runner.go= — the last two found via a broader
   sweep during this chunk, confirmed in scope before fixing) now goes
   through =a.broker()= instead of the concrete client. Several of these
   fixes also collapsed duplicated prefer-snapshot-fall-back-to-broker
   logic into reuse of existing =Account= methods
   (=ListOpenTrades=/=CloseTrade=/=GetAccountSummary=) instead of a third
   copy of the same fallback pattern.
6. *Resolved as moot, no code change, 2026-07-21.* Was: "=AccountSnapshot='s
   role once fills flow through =StreamTransactions= uniformly — likely
   narrows to continuously-updating figures (unrealized PnL, margin)
   rather than trade-ledger reconciliation." That premise assumed live
   would adopt chunk 4's =StreamTransactions=-consuming ledger-application
   flow (=drainBrokerFills=/=txEventToAccountEvent= writing into
   =Account.Lots=) the way backtest did. Chunk 5 explicitly decided
   against that — live has no local ledger by design, OANDA is
   authoritative. Traced =service/account_snapshot.go= to confirm: it's
   fed entirely by polling =GetAccountChanges= (=pollLoop=/=applyChanges=),
   never =StreamTransactions=, and its cache already holds *both* the
   open-trades map *and* the continuously-updating figures in one struct —
   because for live, this cache *is* the only ledger there is. There was
   never a separate trade-ledger reconciliation for =AccountSnapshot= to
   narrow away from; the scenario this chunk was written to respond to
   never materializes, by chunk 5's own design. Nothing to implement.
7. *Done, 2026-07-21.* "Move risk/sizing math onto =Account=, shared by
   backtest and live." Traced the code first: backtest already sized
   through =account.Account.SizePosition= (=account/account_sizing.go=,
   called from =planner.Plan= → =finalize=). Live had *two* sizing-adjacent
   code paths, but only one mattered — the candle-strategy live path
   (=service/candle_strategy_adapter.go=) goes through the same
   =planner.Plan=, but =livePlanContext.Account()= returns =nil= by design,
   so =SizePosition= is skipped and the resulting zero =Units= is silently
   discarded (=LiveOpenRequest= has no =Units= field at all) — dead weight,
   not a real second implementation. The value that actually reached OANDA
   came from =service/orders.go='s =PlaceMarketOrder=: a separate,
   hand-rolled =float64= reimplementation of the same risk-budget math,
   plus =MaxUnits=/=MaxPositionUSD= caps =SizePosition= didn't have, and no
   margin check (relied on OANDA to reject).

   Rather than build a throwaway =Account= for live just to call the
   method, extracted the math to operate on the five scalars it actually
   reads (=Equity=, =MarginUsed=, =FreeMargin=, =RiskFraction=,
   =Currency=) instead of a full ledger: new =account.SizingInputs= struct;
   =riskBudget=/=lossPerUnit=/=marginRequiredPerUnit=/=availableMargin=/
   =unitsByRisk=/=unitsByMargin= now take =SizingInputs= (was =*Account=);
   =SizePosition= is now an exported package-level function
   (=account.SizePosition(in SizingInputs, req *OpenRequest) error=), and
   =Account.SizePosition= is a one-line wrapper via a new
   =(acct *Account) sizingInputs()= helper — every backtest call site
   (=planner/planner.go=) unchanged. Also extracted
   =quoteToAccountRateFor(currency, inst, price)= from
   =(acct *Account) quoteToAccountRate= for the same reason.
   =service/orders.go='s =PlaceMarketOrder= now builds a =SizingInputs=
   straight from the OANDA account summary (=NAV=→=Equity=,
   =MarginUsed=→=MarginUsed=, =MarginAvail=→=FreeMargin=) and calls
   =account.SizePosition=; =MaxUnits=/=MaxPositionUSD= stay as
   =service/orders.go=-local post-caps (order-level constraints, not core
   risk/margin sizing; backtest has no equivalent config today). The dead
   =livePlanContext.Account() == nil= branch in
   =candle_strategy_adapter.go= was deliberately left alone — wiring it up
   would create a second sizing call for the same order.

   Two intentional behavior changes, per "correctness over convenience":
   live orders are now also capped by a margin check (previously
   risk-budget only, relying on OANDA to reject on insufficient margin),
   and a computed size below =inst.MinimumTradeSize= now rejects the order
   instead of the old silent =units = 1= floor. Covered by new tests
   =TestPlaceMarketOrder_MarginCap_BindsBelowRiskSizing= and
   =TestPlaceMarketOrder_MarginCap_InsufficientMargin_Errors= in
   =service/orders_test.go=. =service/orders_test.go='s old
   =TestQuoteToUSDRate_*= tests (covering the deleted
   =service.quoteToUSDRate=) were ported to
   =account/account_sizing_test.go= as =TestQuoteToAccountRateFor_*=, since
   that's where the logic now lives. Verified: =go build ./...=,
   =go vet ./...=, and =go test ./...= (full repo) all clean — backtest
   suite unaffected, confirming the math itself wasn't changed, only its
   callers' shape.

Chunk boundaries and order still open for discussion — this is a first
pass, not a committed plan.
